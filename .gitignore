# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle (also covers each versions/* included build)
build
.gradle
logs
*.log
*.log.gz

# Keep the root release/deployment helpers while ignoring generated build output.
!/build/
/build/*
!/build/*.ps1

# aggregate outputs
/dist/
/reports/

# other (also covers each versions/* included build)
out
eclipse
run
runs
run-data
repo

# 乱七八糟的东西 (其他子项目/目录/临时文件)
.architectury-transformer/
MmdSkin-Bukkit/
MC-MMD-rust/
.ai_progress.md